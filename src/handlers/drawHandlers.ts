import {} from '@nut-tree/nut-js';

export const drawCircle = async (radiusPx: number) => {};

export const drawSquare = async (widthPx: number) => {};

export const drawRectangle = async (widthPx: number, lengthPx: number) => {};
